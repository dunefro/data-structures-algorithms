version: '3'
services:
  db:
    image: 'mysql:latest'
    restart: 'always'
    expose:
      - '3306'
    ports:
      - '3306:3306'
    environment:
       MYSQL_ROOT_PASSWORD: 'qwerty'
       MYSQL_DATABASE: 'crm'
      #  MYSQL_USER: 'user'
      #  MYSQL_PASSWORD: 'pass'